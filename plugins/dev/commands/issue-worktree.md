---
description: Create an isolated git worktree from a GitHub issue with automated branch naming, dependency installation, and terminal setup
allowed-tools: Bash(bun *), Bash(gh *), Bash(cd *)
---

## Instructions

Execute **Workflow 8: Issue Worktree** from the `git-workflow` skill.

**Input**: $ARGUMENTS (GitHub issue URL or issue number)
